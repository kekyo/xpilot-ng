#!/bin/bash

case "$1" in
	purge)
	    rm -rf /etc/xpilot-infinity
	    # Remove xpilotinfinity user and group, if they exist
	    id xpilotinfinity >/dev/null 2>&1 && userdel xpilotinfinity
	;;
esac

#DEBHELPER#
