#!/bin/bash

case "$1" in
	purge)
	    rm -f /etc/default/xpilot-infinity-server
	;;
esac

# Normally added by dh_installinit, but included here manually because
# we customize the prerm and therefore run dh_installinit --noscripts.
if [ "$1" = "purge" ] ; then
        update-rc.d xpilot-infinity-server remove >/dev/null
fi

#DEBHELPER#
